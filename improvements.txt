✨ 4. Improved HTML Structure

Replace your Products link with:

<li class="dropdown">
  <button class="dropdown-toggle" aria-expanded="false">
    Products ▾
  </button>
  <ul class="dropdown-menu">
    <li><a href="products.html#lavender">Lavender Soap</a></li>
    <li><a href="products.html#honey">Honey Oat</a></li>
    <li><a href="products.html#rose">Rose Soap</a></li>
    <li><a href="products.html#mint">Mint Soap</a></li>
    <li class="divider"></li>
    <li><a href="products.html">View All Products</a></li>
  </ul>
</li>
🎨 5. Modern CSS (Premium Look)

Add to your CSS:

.dropdown {
  position: relative;
}

.dropdown-toggle {
  background: none;
  border: none;
  font: inherit;
  cursor: pointer;
  padding: 10px 0;
}

.dropdown-menu {
  position: absolute;
  top: 120%;
  left: 0;
  min-width: 220px;
  background: #ffffff;
  border-radius: 12px;
  box-shadow: 0 15px 35px rgba(0,0,0,0.08);
  padding: 10px 0;
  opacity: 0;
  visibility: hidden;
  transform: translateY(10px);
  transition: all 0.25s ease;
  z-index: 999;
}

.dropdown-menu li {
  list-style: none;
}

.dropdown-menu a {
  display: block;
  padding: 12px 20px;
  text-decoration: none;
  color: #333;
  font-weight: 500;
  transition: background 0.2s ease;
}

.dropdown-menu a:hover {
  background: #f5f5f5;
}

.dropdown:hover .dropdown-menu {
  opacity: 1;
  visibility: visible;
  transform: translateY(0);
}

.divider {
  height: 1px;
  background: #eee;
  margin: 8px 0;
}
📱 6. Mobile Fix (IMPORTANT)

For mobile, disable hover and use click:

Add JS:

const toggle = document.querySelector('.dropdown-toggle');
const menu = document.querySelector('.dropdown-menu');

toggle.addEventListener('click', () => {
  const expanded = toggle.getAttribute('aria-expanded') === 'true';
  toggle.setAttribute('aria-expanded', !expanded);
  menu.style.opacity = expanded ? '0' : '1';
  menu.style.visibility = expanded ? 'hidden' : 'visible';
});
🧠 7. Make It Conversion-Focused

Inside dropdown:

Add:

⭐ Best Seller tag

🔥 Limited Edition

💚 90%+ Natural badge

Example:

<li>
  <a href="products.html#lavender">
    Lavender Soap <span class="badge">Best Seller</span>
  </a>
</li>

Add badge CSS:

.badge {
  background: #9CAF88;
  color: white;
  font-size: 11px;
  padding: 3px 8px;
  border-radius: 20px;
  margin-left: 8px;
}

This subtly increases clicks.

🚀 8. Advanced Upgrade (If You Want Elite Look)

Add:

backdrop-filter: blur(8px);
background: rgba(255,255,255,0.85);

Makes it feel modern and premium.